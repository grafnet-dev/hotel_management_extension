<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_form_hotel_police_form" model="ir.ui.view">
        <field name="name">hotel.police.form.view.form</field>
        <field name="model">hotel.police.form</field>
        <field name="arch" type="xml">
            <form string="Fiche de police - Enregistrement Client">
                
                <!-- Informations occupant -->
             <group string="🧍 Identité de l’occupant">
                    <field name="first_name" string="Prénom"/>
                    <field name="last_name" string="Nom"/>
                    <field name="gender" string="Sexe"/>
                    <field name="date_of_birth" string="Date de naissance"/>
                    <field name="place_of_birth" string="Lieu de naissance"/>
                    <field name="nationality" string="Nationalité"/>
                    <field name="street" string="Adresse"/>
                    <field name="zip" string="Code postal"/>
                    <field name="city" string="Ville"/>
                    <field name="country_id" string="Pays"/>
                </group>

                <!-- Pièce d'identité -->
                <group string="🪪 Pièce d’identité">
                    <field name="id_type" string="Type de pièce"/>
                    <field name="id_number" string="Numéro de la pièce"/>
                    <field name="id_issue_date" string="Date de délivrance"/>
                    <field name="id_expiry_date" string="Date d’expiration"/>
                    <field name="id_issue_place" string="Lieu de délivrance"/>
                    <field name="id_document_file" string="Fichier de la pièce" filename="id_document_filename"/>
                </group>

                <!-- Informations sur le séjour -->
                <group string="🏨 Informations sur le séjour">
                    <field name="arrival_date_time" string="Arrivée prévue" readonly="1"/>
                    <field name="departure_date_time" string="Départ prévu" readonly="1"/>
                    <field name="actual_arrival_date_time" string="Arrivée réelle"/>
                    <field name="actual_departure_date_time" string="Départ réel"/>
                    <field name="number_of_guests" string="Nombre d’occupants"/>
                    <field name="room_id" string="Chambre attribuée"/>
                </group>

                <!-- Informations complémentaires -->
                <group string="📋 Informations complémentaires">
                    <field name="stay_purpose" string="Motif du séjour"/>
                    <field name="arrival_transport" string="Moyen de transport à l’arrivée"/>
                    <field name="signature" string="Signature du client"/>
                    <field name="signature_date" string="Date de signature"/>
                </group>
                <!-- Observations -->
                <group string="Observations">
                    <field name="notes"/>
                </group>

                <!-- Footer -->
                <footer>
                    <button name="action_validate_police_form"
                        string="Valider et passer au Check-In"
                        type="object"
                        class="btn-primary" />
                    <button string="Annuler"
                        special="cancel"
                        class="btn-secondary" />
                </footer>

            </form>
        </field>
    </record>
</odoo>