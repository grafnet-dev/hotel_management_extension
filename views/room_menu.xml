<odoo>
    <!-- Déclare d'abord le menu racine -->
    <menuitem id="menu_room_list_root"
        name="Chambres"
        sequence="10" />

    <!-- Ensuite les actions -->
    <record id="action_test_debug" model="ir.actions.client">
        <field name="name">Test Debug</field>
        <field name="tag">test_debug_action</field>
    </record>

    <record id="action_room_list" model="ir.actions.client">
        <field name="name">Liste des chambres</field>
        <field name="tag">room_list_action</field>
    </record>

    <!-- Ensuite les sous-menus -->
    <menuitem id="menu_test_debug"
        name="🔥 Test Debug"
        parent="menu_room_list_root"
        action="action_test_debug"
        sequence="0" />

    <menuitem id="menu_room_list"
        name="Liste des chambres (OWL)"
        parent="menu_room_list_root"
        action="action_room_list"
        sequence="1" />
</odoo>
