<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
  <t t-name="hotel_management_extension.ReservationDetail">
    <div class="min-h-screen bg-gray-50">
      <!-- Header avec navigation -->
      <div class="bg-white shadow-sm sticky top-0 z-10">
        <div class="max-w-7xl mx-auto px-6 py-4">
          <div class="flex items-center justify-between">
            <div class="flex items-center space-x-4">
              <button t-on-click="props.onBack" 
                      class="flex items-center space-x-2 text-gray-600 hover:text-cyan-700 transition-colors">
                <span class="text-lg">←</span>
                <span class="font-medium">Retour à la liste</span>
              </button>
              <div class="h-6 border-l border-gray-300"/>
              <div>
                <h1 class="text-2xl font-bold text-cyan-900">Détails de la réservation</h1>
                <div class="flex items-center space-x-2 mt-1">
                  <span class="text-amber-600 font-medium" t-esc="props.booking.group_code"/>
                  <span class="text-gray-400">•</span>
                  <span class="text-gray-600" t-esc="props.booking.client.name"/>
                </div>
              </div>
            </div>
            
            <div class="flex items-center space-x-3">
              <button class="bg-white border border-gray-300 hover:bg-gray-50 text-gray-700 px-4 py-2 rounded-lg transition-colors">
                ✏️ Modifier
              </button>
              <button class="bg-red-50 hover:bg-red-100 text-red-600 px-4 py-2 rounded-lg transition-colors">
                🗑️ Annuler
              </button>
              <button class="bg-amber-600 hover:bg-amber-700 text-white px-6 py-2 rounded-lg font-medium transition-colors">
                💾 Sauvegarder
              </button>
            </div>
          </div>
        </div>
      </div>

      <div class="max-w-7xl mx-auto px-6 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

          <!-- Colonne principale -->
          <div class="lg:col-span-2 space-y-6">

            <!-- Informations client -->
            <div class="bg-white rounded-xl shadow-sm">
              <div class="px-6 py-4 border-b border-gray-100">
                <h2 class="text-lg font-semibold text-gray-900 flex items-center">
                  <span class="w-8 h-8 bg-cyan-100 rounded-lg flex items-center justify-center mr-3">
                    👤
                  </span>
                  Informations client
                </h2>
              </div>
              <div class="p-6">
                <div class="flex items-start space-x-6">
                  <!-- Avatar -->
                  <div class="flex-shrink-0">
                    <div class="w-16 h-16 bg-gradient-to-br from-cyan-900 to-cyan-700 rounded-full flex items-center justify-center text-white font-bold text-xl">
                      <t t-esc="props.booking.client.name.split(' ').map(n => n[0]).join('').substring(0, 2)"/>
                    </div>
                    <div class="mt-3 text-center">
                      <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-amber-100 text-amber-800">
                        <t t-esc="props.booking.client.membership_status"/>
                      </span>
                    </div>
                  </div>

                  <!-- Informations -->
                  <div class="flex-1 grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="space-y-4">
                      <div>
                        <label class="text-sm font-medium text-gray-500 uppercase tracking-wide">Nom complet</label>
                        <p class="mt-1 text-lg font-semibold text-gray-900" t-esc="props.booking.client.name"/>
                      </div>
                      
                      <div>
                        <label class="text-sm font-medium text-gray-500 uppercase tracking-wide">Téléphone</label>
                        <p class="mt-1 text-gray-900 flex items-center">
                          <span class="mr-2">📞</span>
                          <t t-esc="props.booking.client.phone"/>
                        </p>
                      </div>
                      
                      <div>
                        <label class="text-sm font-medium text-gray-500 uppercase tracking-wide">Email</label>
                        <p class="mt-1 text-gray-900 flex items-center">
                          <span class="mr-2">✉️</span>
                          <t t-esc="props.booking.client.email"/>
                        </p>
                      </div>
                    </div>
                    
                    <div class="space-y-4">
                      <div>
                        <label class="text-sm font-medium text-gray-500 uppercase tracking-wide">Nationalité</label>
                        <p class="mt-1 text-gray-900" t-esc="props.booking.client.nationality"/>
                      </div>
                      
                      <t t-if="props.booking.client.passport_no">
                        <div>
                          <label class="text-sm font-medium text-gray-500 uppercase tracking-wide">Passeport</label>
                          <p class="mt-1 text-gray-900" t-esc="props.booking.client.passport_no"/>
                        </div>
                      </t>
                      
                      <div>
                        <label class="text-sm font-medium text-gray-500 uppercase tracking-wide">Points fidélité</label>
                        <p class="mt-1 text-amber-600 font-semibold">
                           pts
                        </p>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Séjours -->
            <div class="bg-white rounded-xl shadow-sm">
              <div class="px-6 py-4 border-b border-gray-100">
                <h2 class="text-lg font-semibold text-gray-900 flex items-center justify-between">
                  <span class="flex items-center">
                    <span class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center mr-3">
                      🛏️
                    </span>
                    Séjours (<t t-esc="props.booking.stays.length"/>)
                  </span>
                  <span class="text-sm text-gray-500">
                    Total: <span class="font-bold text-amber-600">
                      k FCFA
                    </span>
                  </span>
                </h2>
              </div>
              <div class="p-6 space-y-6">
                <t t-foreach="props.booking.stays" t-as="stay" t-key="stay.id">
                  <div class="border border-gray-200 rounded-lg p-6 hover:border-cyan-300 transition-colors">

                    <!-- Header du séjour -->
                    <div class="flex items-center justify-between mb-4">
                      <div class="flex items-center space-x-3">
                        <div class="w-10 h-10 bg-gradient-to-br from-cyan-600 to-cyan-800 rounded-lg flex items-center justify-center text-white font-bold">
                          <t t-esc="stay.room_name.split(' ')[1]"/>
                        </div>
                        <div>
                          <h3 class="font-semibold text-gray-900" t-esc="stay.room_name"/>
                          <p class="text-sm text-gray-600" t-esc="stay.room_type"/>
                        </div>
                      </div>
                      
                      <div class="flex items-center space-x-4">
                        <!-- État de la fiche police -->
                        <t t-if="state.policeForms and state.policeForms[stay.id]">
                          <div class="flex items-center space-x-2">
                            <div class="w-2 h-2 bg-green-500 rounded-full"></div>
                            <span class="text-sm text-green-600 font-medium">Fiche complétée</span>
                          </div>
                        </t>

                        <!-- Bouton Check-in  -->
                        <t t-if="stay.status === 'pending'">
                          <button
                            t-on-click="() => this.triggerCheckin(stay)"
                            class="inline-flex items-center px-6 py-2.5 bg-gradient-to-r from-cyan-600 to-cyan-700 hover:from-cyan-700 hover:to-cyan-800 text-white font-semibold rounded-lg shadow-md hover:shadow-lg transform hover:scale-105 transition-all duration-200 ease-in-out"
                          >
                            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                             🏨 Check-in
                          </button>
                        </t>
                        
                        <div class="text-right">
                          <div class="text-lg font-bold text-amber-600">
                            
                          </div>
                          <t t-set="status" t-value="stay.status"/>
                          <span t-att-class="'inline-flex items-center px-2 py-1 rounded-full text-xs font-medium ' + 
                            (status === 'confirmed' ? 'bg-green-100 text-green-800' : 
                             status === 'pending' ? 'bg-yellow-100 text-yellow-800' : 
                             status === 'in_progress' ? 'bg-blue-100 text-blue-800' : 
                             'bg-gray-100 text-gray-800')">
                            <t t-esc="status === 'confirmed' ? 'Confirmé' : status === 'pending' ? 'En attente' : status === 'in_progress' ? 'En cours' : status"/>
                          </span>
                        </div>
                      </div>
                    </div>

                    <!-- Détails du séjour -->
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">

                      <!-- Dates -->
                      <div class="space-y-3">
                        <h4 class="text-sm font-medium text-gray-700 uppercase tracking-wide">Période</h4>
                        <div class="space-y-2">
                          <div class="flex items-center text-sm">
                            <span class="w-16 text-gray-500">Arrivée:</span>
                            <span class="font-medium">
                              <t t-esc="new Date(stay.checkin_date).toLocaleDateString('fr-FR', {
                                weekday: 'short', 
                                day: '2-digit', 
                                month: 'short',
                                hour: '2-digit',
                                minute: '2-digit'
                              })"/>
                            </span>
                          </div>
                          <div class="flex items-center text-sm">
                            <span class="w-16 text-gray-500">Départ:</span>
                            <span class="font-medium">
                              <t t-esc="new Date(stay.checkout_date).toLocaleDateString('fr-FR', {
                                weekday: 'short', 
                                day: '2-digit', 
                                month: 'short',
                                hour: '2-digit',
                                minute: '2-digit'
                              })"/>
                            </span>
                          </div>
                          <t t-set="nights" t-value="Math.ceil((new Date(stay.checkout_date) - new Date(stay.checkin_date)) / (1000 * 60 * 60 * 24))"/>
                          <div class="text-xs text-amber-600 font-medium">
                            <t t-esc="nights"/> nuit<t t-if="nights > 1">s</t>
                          </div>
                        </div>
                      </div>

                      <!-- Occupant -->
                      <div class="space-y-3">
                        <h4 class="text-sm font-medium text-gray-700 uppercase tracking-wide">Occupant</h4>
                        <div>
                          <p class="font-medium text-gray-900" t-esc="stay.occupant_name"/>
                          <p class="text-sm text-gray-600 capitalize" t-esc="stay.reservation_type"/>
                        </div>
                      </div>

                      <!-- Services spéciaux -->
                      <div class="space-y-3">
                        <h4 class="text-sm font-medium text-gray-700 uppercase tracking-wide">Early Checkin / Late Checkout</h4>
                        <div class="space-y-1">
                          <t t-if="stay.early_checkin_requested">
                            <span class="inline-flex items-center px-2 py-1 rounded-md text-xs font-medium bg-orange-100 text-orange-800 mr-1 mb-1">
                              🌅 Check-in <t t-esc="stay.early_checkin_hour"/>h
                            </span>
                          </t>
                          <t t-if="stay.late_checkout_requested">
                            <span class="inline-flex items-center px-2 py-1 rounded-md text-xs font-medium bg-purple-100 text-purple-800 mr-1 mb-1">
                              🌆 Check-out <t t-esc="stay.late_checkout_hour"/>h
                            </span>
                          </t>
                          <t t-if="stay.extra_night_required">
                            <span class="inline-flex items-center px-2 py-1 rounded-md text-xs font-medium bg-blue-100 text-blue-800 mr-1 mb-1">
                              🛏️ Nuit supplémentaire
                            </span>
                          </t>
                          <t t-if="stay.was_requalified_flexible">
                            <span class="inline-flex items-center px-2 py-1 rounded-md text-xs font-medium bg-cyan-100 text-cyan-800 mr-1 mb-1">
                              🔄 Requalifié flexible
                            </span>
                          </t>
                        </div>
                      </div>
                    </div>

                    <!-- Section Facturation pour chaque séjour -->
                    <div class="mt-6 pt-4 border-t border-gray-100">
                      <h4 class="text-md font-semibold text-gray-900 mb-4">Facturation</h4>

                      <!-- Totaux de consommation -->
                      <div class="bg-gray-50 rounded-lg p-4 mb-4">
                        <div class="grid grid-cols-2 gap-4">
                          <div>
                            <span class="text-sm text-gray-600">Total consommation :</span>
                            <p class="font-semibold text-gray-900">
                              <t t-esc="stay.consumption_total ? stay.consumption_total.toFixed(2) : '0.00'"/> FCFA
                            </p>
                          </div>
                          <div>
                            <span class="text-sm text-gray-600">Montant total à facturer :</span>
                            <p class="font-semibold text-amber-600">
                              <t t-esc="stay.total_invoice_amount ? stay.total_invoice_amount.toFixed(2) : '0.00'"/> FCFA
                            </p>
                          </div>
                        </div>
                      </div>

                      <!-- Détail des consommations -->
                      <div class="space-y-4">
                        <!-- Restauration -->
                        <t t-if="stay.food_booking_lines and stay.food_booking_lines.length">
                          <div class="bg-white border border-gray-200 rounded-lg p-4">
                            <h5 class="font-medium text-gray-900 mb-2 flex items-center">
                              <span class="mr-2">🍽️</span>
                              Restauration
                            </h5>
                            <div class="space-y-1">
                              <t t-foreach="stay.food_booking_lines" t-as="line" t-key="line.id">
                                <div class="flex justify-between items-center text-sm">
                                  <span class="text-gray-700">
                                    <t t-esc="line.name"/> (×<t t-esc="line.quantity"/>)
                                  </span>
                                  <span class="font-medium text-gray-900">
                                    <t t-esc="(line.quantity * line.unit_price).toFixed(2)"/> FCFA
                                  </span>
                                </div>
                              </t>
                            </div>
                          </div>
                        </t>

                        <!-- Évènements -->
                        <t t-if="stay.event_booking_lines and stay.event_booking_lines.length">
                          <div class="bg-white border border-gray-200 rounded-lg p-4">
                            <h5 class="font-medium text-gray-900 mb-2 flex items-center">
                              <span class="mr-2">🎉</span>
                              Évènements
                            </h5>
                            <div class="space-y-1">
                              <t t-foreach="stay.event_booking_lines" t-as="line" t-key="line.id">
                                <div class="flex justify-between items-center text-sm">
                                  <span class="text-gray-700">
                                    <t t-esc="line.name"/> (×<t t-esc="line.quantity"/>)
                                  </span>
                                  <span class="font-medium text-gray-900">
                                    <t t-esc="(line.quantity * line.unit_price).toFixed(2)"/> FCFA
                                  </span>
                                </div>
                              </t>
                            </div>
                          </div>
                        </t>

                        <!-- Services additionnels -->
                        <t t-if="stay.service_booking_lines and stay.service_booking_lines.length">
                          <div class="bg-white border border-gray-200 rounded-lg p-4">
                            <h5 class="font-medium text-gray-900 mb-2 flex items-center">
                              <span class="mr-2">🛎️</span>
                              Services additionnels
                            </h5>
                            <div class="space-y-1">
                              <t t-foreach="stay.service_booking_lines" t-as="line" t-key="line.id">
                                <div class="flex justify-between items-center text-sm">
                                  <span class="text-gray-700">
                                    <t t-esc="line.name"/> (×<t t-esc="line.quantity"/>)
                                  </span>
                                  <span class="font-medium text-gray-900">
                                    <t t-esc="(line.quantity * line.unit_price).toFixed(2)"/> FCFA
                                  </span>
                                </div>
                              </t>
                            </div>
                          </div>
                        </t>
                      </div>
                    </div>

                    <!-- Notes -->
                    <t t-if="stay.notes">
                      <div class="mt-4 pt-4 border-t border-gray-100">
                        <h4 class="text-sm font-medium text-gray-700 mb-2">Notes</h4>
                        <p class="text-sm text-gray-600 bg-gray-50 rounded-lg p-3" t-esc="stay.notes"/>
                      </div>
                    </t>
                  </div>
                </t>
              </div>
            </div>
          </div>

          <!-- Sidebar -->
          <div class="space-y-6">

            <!-- Résumé -->
            <div class="bg-white rounded-xl shadow-sm">
              <div class="px-6 py-4 border-b border-gray-100">
                <h2 class="text-lg font-semibold text-gray-900">Résumé</h2>
              </div>
              <div class="p-6 space-y-4">
                <div class="flex justify-between items-center">
                  <span class="text-gray-600">Code réservation</span>
                  <span class="font-medium text-amber-600" t-esc="props.booking.group_code"/>
                </div>
                
                <div class="flex justify-between items-center">
                  <span class="text-gray-600">Date de réservation</span>
                  
                </div>
                
                <div class="flex justify-between items-center">
                  <span class="text-gray-600">Nombre de séjours</span>
                  <span class="font-medium" t-esc="props.booking.stays.length"/>
                </div>
                
                <t t-set="totalNights" t-value="props.booking.stays.reduce((sum, stay) => sum + Math.ceil((new Date(stay.checkout_date) - new Date(stay.checkin_date)) / (1000 * 60 * 60 * 24)), 0)"/>
                <div class="flex justify-between items-center">
                  <span class="text-gray-600">Total nuits</span>
                  <span class="font-medium" t-esc="totalNights"/>
                </div>
                
                <hr class="my-4"/>
                
                <div class="flex justify-between items-center text-lg">
                  <span class="font-semibold text-gray-900">Total</span>
                  <span class="font-bold text-amber-600">
                    k FCFA
                  </span>
                </div>
              </div>
            </div>

            <!-- Actions rapides -->
            <div class="bg-white rounded-xl shadow-sm">
              <div class="px-6 py-4 border-b border-gray-100">
                <h2 class="text-lg font-semibold text-gray-900">Actions rapides</h2>
              </div>
              <div class="p-6 space-y-3">
                <button class="w-full bg-cyan-600 hover:bg-cyan-700 text-white px-4 py-3 rounded-lg font-medium transition-colors flex items-center justify-center">
                  🏨 Action
                </button>
                
                <button class="w-full bg-amber-600 hover:bg-amber-700 text-white px-4 py-3 rounded-lg font-medium transition-colors flex items-center justify-center">
                  📧 Envoyer confirmation
                </button>
                
                <button class="w-full bg-white hover:bg-gray-50 text-gray-700 px-4 py-3 rounded-lg font-medium transition-colors border border-gray-300 flex items-center justify-center">
                  📄 Générer facture
                </button>
                
                <button class="w-full bg-white hover:bg-gray-50 text-gray-700 px-4 py-3 rounded-lg font-medium transition-colors border border-gray-300 flex items-center justify-center">
                  📞 Contacter client
                </button>
              </div>
            </div>

            <!-- Historique -->
            <div class="bg-white rounded-xl shadow-sm">
              <div class="px-6 py-4 border-b border-gray-100">
                <h2 class="text-lg font-semibold text-gray-900">Historique</h2>
              </div>
              <div class="p-6">
                <div class="space-y-4">
                  <div class="flex items-start space-x-3">
                    <div class="w-2 h-2 bg-green-500 rounded-full mt-2 flex-shrink-0"/>
                    <div class="flex-1">
                      <p class="text-sm font-medium text-gray-900">Réservation créée</p>
                      
                    </div>
                  </div>
                  
                  <t t-foreach="props.booking.stays.filter(stay => stay.status === 'confirmed')" t-as="stay" t-key="'confirmed_' + stay.id">
                    <div class="flex items-start space-x-3">
                      <div class="w-2 h-2 bg-blue-500 rounded-full mt-2 flex-shrink-0"/>
                      <div class="flex-1">
                        <p class="text-sm font-medium text-gray-900">Séjour confirmé</p>
                        <p class="text-xs text-gray-500" t-esc="stay.room_name"/>
                      </div>
                    </div>
                  </t>
                  
                  <t t-foreach="props.booking.stays.filter(stay => stay.status === 'in_progress')" t-as="stay" t-key="'progress_' + stay.id">
                    <div class="flex items-start space-x-3">
                      <div class="w-2 h-2 bg-amber-500 rounded-full mt-2 flex-shrink-0"/>
                      <div class="flex-1">
                        <p class="text-sm font-medium text-gray-900">Check-in effectué</p>
                        <p class="text-xs text-gray-500" t-esc="stay.room_name"/>
                      </div>
                    </div>
                  </t>
                </div>
              </div>
            </div>

            <!-- Informations système -->
            <div class="bg-gray-100 rounded-xl p-4">
              <h3 class="text-sm font-medium text-gray-700 mb-3">Informations système</h3>
              <div class="space-y-2 text-xs text-gray-600">
                <div class="flex justify-between">
                  <span>ID Réservation:</span>
                  <span class="font-mono" t-esc="props.booking.id"/>
                </div>
                <div class="flex justify-between">
                  <span>Dernière modification:</span>
                  <span t-esc="new Date().toLocaleDateString('fr-FR')"/>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Modal police form--> 
      <t t-if="state.showPoliceForm">
        <PoliceFormModal
           stay="state.activeStay"
          onValidate.bind="handleFormValidate"
          onCancel.bind="handleCancel"
        />
      </t>

    </div>

  </t>
</templates>