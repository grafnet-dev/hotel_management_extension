<odoo>
    <data noupdate="1">

        <!-- CRON JOB pour calculer les metrics chaque jour à 1h -->
        <record id="ir_cron_hotel_metric_daily" model="ir.cron">
            <field name="name">Hotel Metrics - Daily Compute</field>
            <field name="model_id" ref="model_hotel_metric"/>
            <field name="state">code</field>
            <field name="code">model.action_compute_today()</field>
            <field name="active">True</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall">2025-10-16 01:00:00</field>
        </record>

    </data>
</odoo>
